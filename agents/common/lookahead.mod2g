use commonKnowledge as knowledge.
order=linearall.

module lookAhead {
	forall (Agt).sent( msg(delivering(BlockID)) ) do insert ( delivering(Agt, BlockID) ).
	forall (Agt).sent( msg(notDelivering(BlockID)) ) do delete ( delivering(Agt, BlockID) ).
	
	if a-goal( delivered(BlockID) ) then insert( delivering(me, BlockID) ) + 
		allother.send( msg(delivering(BlockID)) ).
	if bel( delivering(me, BlockID) ), not(a-goal( delivered(BlockID) )) then delete(  ) +
		allother.send(msg(notDelivering(BlockID)) ).
}